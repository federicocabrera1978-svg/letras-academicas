name: keep-alive
on:
  schedule:
    - cron: '0 12 */3 * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        run: |
          curl -s -o /dev/null -w "%{http_code}\n" \
            "https://xjrdjesezseuofjoqpzn.supabase.co/rest/v1/textos?select=id&limit=1" \
            -H "apikey: sb_publishable_c6zrWAExpbRtV6tfrYS2fg_7BpKUO1P"
